{
  "name": "@kids/validation",
  "version": "0.0.0",
  "private": true,
  "description": "Zod schemas — one source of truth for every boundary, plus JSON Schema emission.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo",
    "test": "vitest run --project unit --passWithNoTests"
  },
  "dependencies": {
    "@kids/types": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@kids/shared": "workspace:*"
  }
}
