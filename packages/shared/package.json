{
  "name": "@kids/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-cutting primitives: error taxonomy, Result, redacting logger, Clock, IDs.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo",
    "test": "vitest run --project unit --passWithNoTests"
  },
  "dependencies": {
    "@kids/types": "workspace:*",
    "pino": "catalog:"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.3"
  }
}
