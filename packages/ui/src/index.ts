/**
 * @kids/ui — design tokens and cross-platform primitives.
 *
 * Two audiences with genuinely different requirements share this package:
 *
 *   Child mode  — pre-readers. Meaning is carried by character animation, colour,
 *                 and voice; text is a secondary channel at most. Large touch
 *                 targets, no timed interactions, no error text.
 *   Parent mode — a conventional, information-dense, accessible interface.
 *
 * Tokens are shared. Components are not assumed to be.
 *
 * Phases 4 and 5 deliver the component primitives.
 */

export * from './tokens.js';
