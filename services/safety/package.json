{
  "name": "@kids/safety",
  "version": "0.0.0",
  "private": true,
  "description": "The child-AI safety subsystem: harm taxonomy, configurable policy, deterministic detectors, and the input/output safety pipeline.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo",
    "test": "vitest run --project unit --passWithNoTests"
  },
  "dependencies": {
    "@kids/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.3"
  }
}
